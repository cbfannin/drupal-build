name: <project name>
recipe: drupal9
config:
  webroot: web
  php: 7.4
  via: nginx
  database: mariadb
  xdebug: true
  composer_version: '2.1.11'
services:
  appserver:
    build:
      - composer install
  node:
    type: node
  mailhog:
    type: mailhog
    hogfrom:
      - appserver
tooling:
  npm:
    service: node
  node:
    service: node
proxy:
  mailhog:
    - mail.<project name>.lndo.site
